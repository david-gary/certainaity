apiVersion: apps/v1
kind: Deployment
metadata:
  name: certainaity-worker
  namespace: certainaity
  labels:
    app: certainaity-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: certainaity-worker
  template:
    metadata:
      labels:
        app: certainaity-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
      containers:
        - name: worker
          image: ghcr.io/david-gary/certainaity-worker:latest
          envFrom:
            - configMapRef:
                name: certainaity-config
          env:
            - name: CUDA_VISIBLE_DEVICES
              value: "0"
          volumeMounts:
            - name: output
              mountPath: /output
            - name: weights
              mountPath: /weights
              readOnly: true
          resources:
            requests:
              cpu: "500m"
              memory: "4Gi"
            limits:
              cpu: "4000m"
              memory: "16Gi"
              nvidia.com/gpu: "1"
      volumes:
        - name: output
          persistentVolumeClaim:
            claimName: certainaity-output
        - name: weights
          persistentVolumeClaim:
            claimName: certainaity-weights
      # Schedule worker pods on GPU nodes.
      nodeSelector:
        accelerator: nvidia
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
